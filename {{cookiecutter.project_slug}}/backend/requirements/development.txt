# Include test dependencies (those already include base)
-r test.txt

# Add code quality tools
pylint==1.8.2
flake8==3.5.0

# Add django extensions
django-extensions==2.0.3
Werkzeug==0.14.1
